package client

import (
	"context"
	"errors"

	"github.com/srepio/sdk/types"
)

// Get all scenarioa metdata
func (c *Client) GetMetadata(ctx context.Context) (types.Metadata, error) {
	return nil, errors.New("not implemented")
}
